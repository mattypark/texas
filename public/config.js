/* Optional. Add a Google Maps API key with the Street View Static API enabled and
   every house card shows a real photo of that house instead of the drawn block map.
   Without a key the page works exactly the same, just with drawings.
   window.HOUSE_RUN_CONFIG = { mapsKey: "YOUR_KEY_HERE" }; */
window.HOUSE_RUN_CONFIG = { mapsKey: "" };
